# JobScout V3 Dependencies
# Python >= 3.10

# Google ADK & Gemini
google-adk>=1.0.0
google-genai>=1.0.0

# Core utilities
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
rich>=13.0.0

# Resume import (PDF/DOCX) and document generation
pypdf>=4.0.0
python-docx>=1.0.0

# Data validation
pydantic>=2.0.0

# Local embeddings — no API key, no network, no GPU. Static distilled vectors:
# tokenizers and numpy, no torch, ~30MB model. Optional: without it the
# pipeline needs a Gemini key for scoring. See config.EMBEDDING_BACKEND.
model2vec>=0.9.0

# UI (app.py) — the local Streamlit front end, R25
# 1.31 is the floor for `st.number_input(value=None)`, which is how the
# preferences screen keeps "not answered" distinct from "zero years".
streamlit>=1.31.0

# API (api/main.py) — what the React front end talks to, and what ships hosted.
#
# These were used by `api/main.py`, documented in the README, and launched by
# `.claude/launch.json` while being declared in no manifest at all. They work
# here because `google-adk` pulls FastAPI in transitively — which is not a
# dependency, it is a coincidence that a version bump can end.
#
# R80's shape exactly: fine where the author is, missing from the manifest,
# and invisible until something builds a clean environment. A container is
# that clean environment.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Optional: Development
# pytest>=7.0.0
# ruff>=0.4.0
